# AI Provider API Keys
# You need to set at least one of these to use the corresponding AI provider.
OPENAI_API_KEY=""
ANTHROPIC_API_KEY=""
GOOGLE_API_KEY=""

# Optional: OpenAI API base URL (for custom endpoints or proxies)
# OPENAI_BASE_URL="https://api.openai.com/v1"

# Optional: OpenAI API timeout in milliseconds
# OPENAI_TIMEOUT_MS="60000"

# Optional: Disable automatic inclusion of project documentation (neopilot.md)
# NEOPILOT_DISABLE_PROJECT_DOC="1"

# Optional: Enable quiet mode (non-interactive, only prints final output)
# NEOPILOT_QUIET_MODE="1"

# Optional: Enable Neopilot development mode (for local development)
# NEOPILOT_DEV="1"

# Node.js environment (e.g., development, production)
# NODE_ENV="development"

# Timezone for Docker containers (e.g., America/New_York)
# TZ="UTC"
